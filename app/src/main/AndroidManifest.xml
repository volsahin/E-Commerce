<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.telpa.ecommerce">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ScreenAActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ScreenBActivity" />
        <activity android:name=".activities.ScreenHActivity" />
        <activity android:name=".activities.ScreenCActivity" />
        <activity android:name=".activities.ScreenIActivity" />
        <activity android:name=".activities.ScreenJActivity" />
        <activity android:name=".activities.ScreenEActivity" />
        <activity android:name=".activities.ScreenFActivity" />
        <activity android:name=".activities.BasketActivity" />
        <activity android:name=".activities.ScreenDActivity" />
        <activity android:name=".activities.BasketActivity" />
        <activity android:name=".activities.ScreenKActivity" />
        <activity android:name=".activities.ScreenGActivity"></activity>
    </application>

</manifest>